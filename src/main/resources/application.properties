# Allow external connections (Render requires 0.0.0.0)
server.address=0.0.0.0

# Use Render’s dynamic port; fallback to 9092 locally
server.port=${PORT:9092}

# Vault file location
vault.path=/data/vault.spm
